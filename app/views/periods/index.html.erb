<h1 class='title'>期・メンバーシップ　一覧</h1>

<table class='table'>
  <thead>
    <tr>
      <th>期</th>
      <th>メンバー種別</th>
      <th>名前</th>
      <th>メンバー数</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @periods.each do |period| %>
      <tr>
        <td><%= period.number %></td>
        <td><%= period.code %></td>
        <td><%= period.explanation %></td>
        <td><%= period.memberships.count %></td>
        <td><%= link_to '詳細を見る', period %></td>
        <td><%= link_to '編集', edit_period_path(period) %></td>
        <td><%= link_to '削除', period, method: :delete, data: { confirm: '本当に削除してもよろしいですか？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_period_path %>
